== dev-version=1
COLUMN TABLE "com.sap.hana.example::test_name" ( 
  "ID" INTEGER, 
  "FULLNAME" NVARCHAR(256),
  PRIMARY KEY ("ID") 
) 

== migration=2 
ALTER TABLE "com.sap.hana.example::test_name" ADD ("ACTIVE" INT);
RENAME COLUMN "com.sap.hana.example::test_name"."FULLNAME" TO "NAME";

== version=3
COLUMN TABLE "com.sap.hana.example::test_name" ( 
  "ID" INTEGER, 
  "NAME" NVARCHAR(256),
  "ACTIVE" INT,
  PRIMARY KEY ("ID")  
) 